# Stop lombok from looking in parent directories for more config.
config.stopBubbling = true

# Emit @java.beans.ConstructorProperties on generated constructors so Jackson 3
# can use them as a properties-based creator for immutable @Value DTOs
# (replaces @Jacksonized, which still generates Jackson 2 packages).
lombok.anyConstructor.addConstructorProperties = true
